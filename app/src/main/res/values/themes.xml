<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Jaarchi" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Support for Theme.Material3.DayNight.NoActionBar fallback reference -->
    <style name="Theme.Material3.DayNight.NoActionBar" parent="Theme.Jaarchi" />
</resources>
